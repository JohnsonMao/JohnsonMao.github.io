schema: spec-driven
locale: tw

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Project: Personal blog (JohnsonMao.github.io).
  Tech stack: Astro, TypeScript, Tailwind CSS v4, Content Collections, pnpm.
  No UI framework by default; use Astro islands only where interactivity is needed.
  Deploy target: static site (e.g. GitHub Pages).
  Structure: src/pages, src/content/blog, src/layouts, src/components, public.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
