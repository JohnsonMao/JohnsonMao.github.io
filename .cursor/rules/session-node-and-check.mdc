---
description: 對話開始用 Node 20.19.4，實作完成後跑 pnpm check
alwaysApply: true
---

# Session：Node 版本與實作後檢查

## 對話開始時

在**第一次需要執行終端指令**或**開始實作/修改程式**時，先切換 Node 版本：

```bash
nvm use 20.19.4
```

之後同一對話中的後續指令可沿用該 shell 的環境，不需重複執行（除非新開終端或上下文已遺失）。

## 實作完成後

在**完成程式實作、修改或重構**後，在宣告完成前：

1. 執行 `pnpm run check`（typecheck、lint、test）。
2. 若有失敗，修正問題後再跑一次，直到通過。
3. 通過後再向使用者回報完成。

未通過 check 前不要聲稱「實作完成」或「已修好」。
